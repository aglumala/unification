This project has been developed using Eclipse ide.
To continue it's development and testing using eclipse IDE,
Unzip and drop the TypeEquiv folder in your eclipse workspace (The directory where eclipse puts your projects)

Use the WSDLParser class for testing the parser and your equivalence algorithms.

The two .jar files contain all the required referenced libraries and the classes for this application.